name: Shell Commands

on: [push]

jobs:

  run-shel-command:
      runs-on: ubuntu-latest
      steps:
          - name: echo a string
            run: echoxxx "Hello work flows"
          - name: multiline script
            run: |
              node -v
              npm -v